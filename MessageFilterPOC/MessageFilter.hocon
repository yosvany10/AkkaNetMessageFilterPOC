akka {
	actor {
		provider = cluster
		
	}

	remote {
			dot-netty.tcp {
				port = 6969
				hostname = localhost
			}
	}

	cluster {
		seed-nodes = ["akka.tcp://MessageFilterSystem@localhost:6969"]
		roles = ["bucket"]
	}
}